{
  "name": "hot-updater-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply hot-updater-db --remote",
    "migrate:local": "wrangler d1 migrations apply hot-updater-db --local"
  },
  "dependencies": {
    "@hot-updater/server": "*",
    "@hot-updater/aws": "*",
    "drizzle-orm": "*",
    "hono": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "*",
    "drizzle-kit": "*",
    "wrangler": "*"
  }
}
